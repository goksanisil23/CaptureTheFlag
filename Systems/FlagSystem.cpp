#include "FlagSystem.h"

FlagSystem::FlagSystem()
{
}

void FlagSystem::step(std::vector<Agent> &agents)
{
    // Intersection logic to check if the agent has overlapped with the flag
    // If so, give the flag and set its property.
}